package vn.org.thn.service.app.quiz.entity;

/** The 2 values {@link Test#getStatus()} can hold in v1 - stored as its {@code name()} in the plain {@code status} column. */
public enum TestStatus {
    ASSIGNED,
    COMPLETED
}
